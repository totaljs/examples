
// Allow only this IP address
// F.restrictions.allow('192.168.11.95');

// Disallow only this IP address
F.restrictions.disallow('192.168.11.95');

// Allow only Chrome
// F.restrictions.allow('user-agent', /Chrome/g);

// Disallow only Chrome
F.restrictions.disallow('user-agent', /Chrome/g);